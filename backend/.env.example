NODE_ENV=development
# Port the backend listens on
PORT=5000
# Example MongoDB URI for local development
MONGO_URI=mongodb://127.0.0.1:27017/servicehive-leads
# Replace with a strong secret in production
JWT_SECRET=your_jwt_secret_here
# Token lifetime (e.g. 30d, 24h)
JWT_EXPIRES_IN=30d
